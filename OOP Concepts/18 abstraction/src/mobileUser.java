public abstract class mobileUser {
    abstract void message();
    void call()
    {
        System.out.println("Call is coming");
    }

    /*
     abstract class is a function by which we can only see the front side notice but we cannot see
     what is happen in the backend.
     we can declare a abstract class by adding abstruct before the class
     we can declare an abstract method by only use first bracket aftet the method name and we need to add
     abstract and there will be no body for an abstract method . we cannot create a object of an abstract
     class. but we can use the reference value of it class. 
     */
}
